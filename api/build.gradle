plugins {
    id 'org.springframework.boot'
}

description = 'api'


bootJar {
     mainClassName = 'com.sudhir.registration.api.ApiApplication'
}

dependencies {
    implementation project(':service')
    implementation 'org.springframework.boot:spring-boot-starter-web'
}